

The `trackEvent` module doesn't exist. I need to create `src/lib/trackEvent.ts` with a simple implementation that logs events (and optionally sends them to an analytics endpoint).

**Plan:**
1. Create `src/lib/trackEvent.ts` — a lightweight function that accepts an event name and properties object, and logs to console. This unblocks the build immediately.

